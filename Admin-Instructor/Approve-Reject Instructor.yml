info:
  name: Approve/Reject Instructor
  type: http
  seq: 3
  tags:
    - Admin/Instructor

http:
  method: PATCH
  url: "{{baseUrl}}https://machinery-ranks-rehab-touring.trycloudflare.com/dashboard/instructors/1/"
  body:
    type: json
    data: '"{\r\n    \"status\": \"approved\"\r\n}"'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}https://machinery-ranks-rehab-touring.trycloudflare.com/dashboard/instructors/1/"
      method: PATCH
      body:
        type: json
        data: '"{\r\n    \"status\": \"approved\"\r\n}"'
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""
  - name: 404 Response
    description: Failure
    request:
      url: "{{baseUrl}}https://machinery-ranks-rehab-touring.trycloudflare.com/dashboard/instructors/1/"
      method: PATCH
      body:
        type: json
        data: '"{\r\n    \"status\": \"approved\"\r\n}"'
    response:
      status: 404
      statusText: Not Found
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""
