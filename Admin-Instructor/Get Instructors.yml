info:
  name: Get Instructors
  type: http
  seq: 2
  tags:
    - Admin/Instructor

http:
  method: GET
  url: "{{baseUrl}}dashboard/instructors/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}https://apparently-improve-pretty-thus.trycloudflare.com/dashboard/instructors/"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""
  - name: 404 Response
    description: Failure
    request:
      url: "{{baseUrl}}https://apparently-improve-pretty-thus.trycloudflare.com/dashboard/instructors/"
      method: GET
    response:
      status: 404
      statusText: Not Found
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""
