info:
  name: Add MCQ Questions
  type: http
  seq: 2
  tags:
    - Admin/MCQ Questions

http:
  method: POST
  url: "{{baseUrl}}https://least-resulting-tennessee-representation.trycloudflare.com/dashboard/mcq-questions/3/"
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value: []
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}https://least-resulting-tennessee-representation.trycloudflare.com/dashboard/mcq-questions/3/"
      method: POST
      body:
        type: multipart-form
        data:
          - name: file
            type: file
            value: []
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""
  - name: 404 Response
    description: Failure
    request:
      url: "{{baseUrl}}https://least-resulting-tennessee-representation.trycloudflare.com/dashboard/mcq-questions/3/"
      method: POST
      body:
        type: multipart-form
        data:
          - name: file
            type: file
            value: []
    response:
      status: 404
      statusText: Not Found
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""
