info:
  name: Delete Question
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/questions/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
