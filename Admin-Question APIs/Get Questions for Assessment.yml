info:
  name: Get Questions for Assessment
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}questions/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
