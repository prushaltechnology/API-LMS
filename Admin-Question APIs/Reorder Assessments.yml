info:
  name: Reorder Assessments
  type: http
  seq: 7

http:
  method: PATCH
  url: "{{baseUrl}}assessments/reorder/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
