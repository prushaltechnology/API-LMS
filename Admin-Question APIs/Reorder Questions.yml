info:
  name: Reorder Questions
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{baseUrl}}questions/reorder/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
