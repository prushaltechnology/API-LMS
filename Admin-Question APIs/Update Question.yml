info:
  name: Update Question
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}questions/1/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
