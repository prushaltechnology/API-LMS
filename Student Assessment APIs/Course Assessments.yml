info:
  name: Course Assessments
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}course/1/assessments/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
