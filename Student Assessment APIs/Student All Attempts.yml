info:
  name: Student All Attempts
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}student-all-attempts/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
