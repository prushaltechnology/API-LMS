info:
  name: Check Chat Access
  type: http
  seq: 8
  tags:
    - User

http:
  method: GET
  url: "{{baseUrl}}https://toolkit-pan-philadelphia-healing.trycloudflare.com/api/check-chat-access/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Success
    request:
      url: "{{baseUrl}}https://toolkit-pan-philadelphia-healing.trycloudflare.com/api/check-chat-access/"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""
  - name: 404 Response
    description: Failure
    request:
      url: "{{baseUrl}}https://toolkit-pan-philadelphia-healing.trycloudflare.com/api/check-chat-access/"
      method: GET
    response:
      status: 404
      statusText: Not Found
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""
