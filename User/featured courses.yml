info:
  name: featured courses
  type: http
  seq: 15

http:
  method: GET
  url: "{{baseUrl}}featured-courses/"
  auth:
    type: bearer

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
